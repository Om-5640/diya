# DIYA API Contract

Generated by scripts/generate_api_contract.py -- do not hand-edit; re-run the script after any api/ change.

OpenAPI version: 3.1.0. API version: 0.1.0-phase4.
Raw machine-readable schema: [openapi.json](openapi.json). Interactive docs: `/docs` on a running server.

## Legacy (Khavda-only, preserved)

These routes existed before the multi-site store (DIYA v2 Phase C) and are thin wrappers that always resolve site_id="khavda" internally (`/` and `/api/health` are global infra routes, not khavda-specific, but grouped here since they are not part of the multi-site feature set). Behavior is unchanged from before Phase C.

### `GET /`

Service pointer. Not part of the dispatch API itself -- just tells a
caller landing on the bare host where to find the interactive docs, the
raw OpenAPI schema, and the health check.

**Response schema (200):**
- `service`: string (required)
- `docs`: string (required)
- `openapi`: string (required)
- `health`: string (required)

**Example:**

*Service pointer*
- Path: `/`
- Response (200):
```json
{
  "service": "DIYA API",
  "docs": "/docs",
  "openapi": "/openapi.json",
  "health": "/api/health"
}
```


### `GET /api/health`

Liveness check. Always returns 200 with ok=true if the process is up
and able to handle requests; no error cases.

**Response schema (200):**
- `ok`: boolean (required)
- `phase`: integer (required)

**Example:**

*Liveness check*
- Path: `/api/health`
- Response (200):
```json
{
  "ok": true,
  "phase": 4
}
```


### `GET /api/runs`

List every precomputed dispatch run for Khavda (legacy, khavda-only).

Returns one entry per (scenario_id, policy) pair found under
data/processed/runs/. No error cases -- an empty list if none exist.

**Response schema (200):**
Array of `RunListEntry`:
- `scenario_id`: string (required)
- `policy`: string (required)
- `name`: string (required)

**Example:**

*All precomputed Khavda runs*
- Path: `/api/runs`
- Response (200):
```json
[
  {
    "scenario_id": "S1",
    "policy": "diesel_only",
    "name": "Normal week"
  },
  {
    "scenario_id": "S1",
    "policy": "mpc",
    "name": "Normal week"
  },
  {
    "scenario_id": "S1",
    "policy": "perfect_foresight",
    "name": "Normal week"
  },
  {
    "scenario_id": "S1",
    "policy": "rule_based",
    "name": "Normal week"
  },
  {
    "scenario_id": "S2",
    "policy": "diesel_only",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S2",
    "policy": "mpc",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S2",
    "policy": "perfect_foresight",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S2",
    "policy": "rule_based",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S3",
    "policy": "diesel_only",
    "name": "Lean season"
  },
  {
    "scenario_id": "S3",
    "policy": "mpc",
    "name": "Lean season"
  },
  {
    "scenario_id": "S3",
    "policy": "perfect_foresight",
    "name": "Lean season"
  },
  {
    "scenario_id": "S3",
    "policy": "rule_based",
    "name": "Lean season"
  }
]
```


### `GET /api/runs/{scenario_id}/{policy}`

Fetch one precomputed dispatch run for Khavda (legacy, khavda-only).

Example: GET /api/runs/S2/mpc

Errors: 404 if no precomputed run exists for that scenario_id/policy pair.

**Path/query parameters:**
- `scenario_id` (path): string — required
- `policy` (path): string — required

**Response schema (200):**
- `scenario_id`: string (required)
- `policy`: string (required)
- `site_id`: string (required)
- `site_name`: string (required)
- `steps`: list[StepResult] (required)
- `kpi`: KPI (required)
- `provenance`: Provenance (required)
- `notes`: string (optional, default='')

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Examples:**

*Existing precomputed run*
- Path: `/api/runs/S2/mpc`
- Response (200):
```json
{
  "scenario_id": "S2",
  "policy": "mpc",
  "site_id": "khavda_01",
  "site_name": "Khavda Off-Grid Microgrid",
  "steps": [
    {
      "t": "2025-09-03T00:00:00+05:30",
      "pv_kw": 0.0,
      "wind_kw": 0.46453875011030227,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": 6.907007896208329,
      "soc_kwh": 64.72946537241228,
      "soc_pct": 53.9412211436769,
      "load_critical_kw": 1.2,
      "load_essential_kw": 6.171546646318631,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R6_AVOID_START",
      "reason_text": "Avoiding a generator start; the battery covers this gap."
    },
    {
      "t": "2025-09-03T01:00:00+05:30",
      "pv_kw": 0.0,
      "wind_kw": 0.0997540869082972,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": 7.084458048787209,
      "soc_kwh": 57.272141110531,
      "soc_pct": 47.72678425877584,
      "load_critical_kw": 1.2,
      "load_essential_kw": 5.984212135695506,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R6_AVOID_START",
      "reason_text": "Avoiding a generator start; the battery covers this gap."
    },
    "... (168 steps total, truncated for this doc)"
  ],
  "kpi": {
    "diesel_l": 403.0769478809622,
    "cost_fuel_inr": 37284.61767898901,
    "cost_total_inr": 41754.75973673548,
    "co2_kg": 1080.2462203209789,
    "renewable_frac": 0.5667797555434724,
    "unserved_critical_kwh": 0.0,
    "unserved_total_kwh": 29.970801425376486,
    "critical_outage_hours": 0.0,
    "dg_starts": 13,
    "dg_run_hours": 67.0,
    "batt_equivalent_full_cycles": 4.4355792895730195,
    "cost_per_delivered_kwh_inr": 16.923106208210587,
    "served_kwh": 2467.3224420512893,
    "solve_ms_mean": 198.40602559522614,
    "solve_ms_p95": 350.1660250003624
  },
  "provenance": {
    "weather_source": "open-meteo-archive",
    "forecast_method": "ar1-lead-scaled-noise-v1",
    "load_method": "synthetic-seeded-v1",
    "diesel_price_source": "PPAC Gujarat retail selling price \u2014 PLACEHOLDER, verify before demo",
    "config_hash": "c8b5f984",
    "generated_at": "2026-09-11T22:59:13.875116+00:00",
    "code_version": "phase3"
  },
  "notes": ""
}
```

*Unknown policy -> 404*
- Path: `/api/runs/S2/does_not_exist`
- Response (404):
```json
{
  "detail": "no precomputed run for S2/does_not_exist"
}
```


### `POST /api/solve_live`

One-shot 72-hour forecast-driven dispatch plan for Khavda, using
real live weather for (lat, lon) where possible (legacy, khavda-only).

Example request body: {"lat": 23.8443, "lon": 69.7317, "soc_pct": 60.0}

Weather falls back live-fetch -> last cached forecast -> scenario replay
if the network is unavailable; provenance.forecast_method in the
response says which was actually used. No error cases -- this endpoint
never raises on a weather-fetch failure, only on malformed input (422).

**Request body schema:**
- `lat`: number (required)
- `lon`: number (required)
- `soc_pct`: number (required)

**Response schema (200):**
- `scenario_id`: string (required)
- `policy`: string (required)
- `site_id`: string (required)
- `site_name`: string (required)
- `steps`: list[StepResult] (required)
- `kpi`: KPI (required)
- `provenance`: Provenance (required)
- `notes`: string (optional, default='')

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*Live 72h forecast-driven plan for Khavda's coordinates*
- Path: `/api/solve_live`
- Request body:
```json
{
  "lat": 23.8443,
  "lon": 69.7317,
  "soc_pct": 60.0
}
```
- Response (200):
```json
{
  "scenario_id": "LIVE",
  "policy": "mpc",
  "site_id": "khavda_01",
  "site_name": "Khavda Off-Grid Microgrid",
  "steps": [
    {
      "t": "2026-09-12T17:00:00+05:30",
      "pv_kw": 16.088683,
      "wind_kw": 0.0,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": -3.8956994,
      "soc_kwh": 75.700914,
      "soc_pct": 63.084095,
      "load_critical_kw": 1.2,
      "load_essential_kw": 10.992984087278119,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 4.4476879068611197e-07,
      "fuel_l": 0.0,
      "reason_code": "R2_SOLAR_SURPLUS",
      "reason_text": "Charging the battery from surplus solar and wind."
    },
    {
      "t": "2026-09-12T18:00:00+05:30",
      "pv_kw": 6.0572645,
      "wind_kw": 0.0,
      "dg_kw": 25.0,
      "dg_on": 1,
      "batt_kw": -8.3916434,
      "soc_kwh": 83.672976,
      "soc_pct": 69.72748,
      "load_critical_kw": 1.2,
      "load_essential_kw": 21.465621064542027,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 8.25,
      "reason_code": "R1_PREPOSITION",
      "reason_text": "Starting diesel early \u2014 low solar and wind expected this evening; preserving battery for the health centre."
    },
    "... (72 steps total, truncated for this doc)"
  ],
  "kpi": {
    "diesel_l": 143.71979325,
    "cost_fuel_inr": 13294.080875625,
    "cost_total_inr": 16272.139887045001,
    "co2_kg": 385.16904591,
    "renewable_frac": 0.5781836486430276,
    "unserved_critical_kwh": 0.0,
    "unserved_total_kwh": 119.0927553,
    "critical_outage_hours": 0.0,
    "dg_starts": 3,
    "dg_run_hours": 18.0,
    "batt_equivalent_full_cycles": 2.20263902,
    "cost_per_delivered_kwh_inr": 17.061522808332956,
    "served_kwh": 953.7331497219923,
    "solve_ms_mean": 533.3223000052385,
    "solve_ms_p95": 533.3223000052385
  },
  "provenance": {
    "weather_source": "open-meteo-forecast-live",
    "forecast_method": "open-meteo-forecast-live",
    "load_method": "synthetic-seeded-v1",
    "diesel_price_source": "PPAC Gujarat retail selling price \u2014 PLACEHOLDER, verify before demo",
    "config_hash": "c8b5f984",
    "generated_at": "2026-09-12T11:48:55.914509+00:00",
    "code_version": "phase4"
  },
  "notes": "Live 72h forecast-driven plan (forecast_method=open-meteo-forecast-live)."
}
```


### `POST /api/resolve`

Re-solve a full Khavda scenario (S1/S2/S3) via rolling MPC with
user-adjusted cost/CO2/reliability weights, k_uncertainty, and diesel
price (legacy, khavda-only).

Example request body: {"scenario_id": "S1", "weights": {"cost": 1.0,
"co2": 1.0, "reliability": 1.0}, "k_uncertainty": 1.0,
"diesel_price_inr_per_l": 92.5}

SLOW: ~60-65s (168 sequential 24h MILP solves) -- see the comment block
above this route for why, and FREEZE_NOTES.md for the accepted timing.

Errors: 404 if scenario_id is not one of the configured scenarios, or if
that scenario has no parquet data on disk.

**Request body schema:**
- `scenario_id`: string (required)
- `weights`: ResolveWeights (optional, default={'cost': 1.0, 'co2': 1.0, 'reliability': 1.0})
- `k_uncertainty`: number (required)
- `diesel_price_inr_per_l`: number (required)

**Response schema (200):**
- `scenario_id`: string (required)
- `policy`: string (required)
- `site_id`: string (required)
- `site_name`: string (required)
- `steps`: list[StepResult] (required)
- `kpi`: KPI (required)
- `provenance`: Provenance (required)
- `notes`: string (optional, default='')

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Examples:**

*Re-solve Khavda scenario S1 with default weights (this is the slow ~60-65s endpoint)*
- Path: `/api/resolve`
- Request body:
```json
{
  "scenario_id": "S1",
  "weights": {
    "cost": 1.0,
    "co2": 1.0,
    "reliability": 1.0
  },
  "k_uncertainty": 1.0,
  "diesel_price_inr_per_l": 92.5
}
```
- Response (200):
```json
{
  "scenario_id": "S1",
  "policy": "mpc",
  "site_id": "khavda_01",
  "site_name": "Khavda Off-Grid Microgrid",
  "steps": [
    {
      "t": "2025-09-17T00:00:00+05:30",
      "pv_kw": 0.0,
      "wind_kw": 0.0,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": 6.771797467039842,
      "soc_kwh": 64.87179213995806,
      "soc_pct": 54.059826783298384,
      "load_critical_kw": 1.2,
      "load_essential_kw": 5.571797467039842,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R6_AVOID_START",
      "reason_text": "Avoiding a generator start; the battery covers this gap."
    },
    {
      "t": "2025-09-17T01:00:00+05:30",
      "pv_kw": 0.0,
      "wind_kw": 0.0,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": 7.246935590835981,
      "soc_kwh": 57.2434388864465,
      "soc_pct": 47.70286573870542,
      "load_critical_kw": 1.2,
      "load_essential_kw": 6.0469355908359805,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R6_AVOID_START",
      "reason_text": "Avoiding a generator start; the battery covers this gap."
    },
    "... (168 steps total, truncated for this doc)"
  ],
  "kpi": {
    "diesel_l": 333.985116135515,
    "cost_fuel_inr": 30893.623242535137,
    "cost_total_inr": 35039.96978304923,
    "co2_kg": 895.0801112431802,
    "renewable_frac": 0.6379641150269475,
    "unserved_critical_kwh": 0.0,
    "unserved_total_kwh": 21.348859835644667,
    "critical_outage_hours": 0.0,
    "dg_starts": 14,
    "dg_run_hours": 53.0,
    "batt_equivalent_full_cycles": 5.315030924580892,
    "cost_per_delivered_kwh_inr": 14.061130865971696,
    "served_kwh": 2491.973804741898,
    "solve_ms_mean": 144.1776964284145,
    "solve_ms_p95": 224.65554500158763
  },
  "provenance": {
    "weather_source": "open-meteo-archive",
    "forecast_method": "ar1-lead-scaled-noise-v1",
    "load_method": "synthetic-seeded-v1",
    "diesel_price_source": "user-override (weights applied via /api/resolve)",
    "config_hash": "c8b5f984",
    "generated_at": "2026-09-12T11:49:21.162610+00:00",
    "code_version": "phase4"
  },
  "notes": "Re-solved via /api/resolve with user-adjusted weights/k_uncertainty/diesel price."
}
```

*Unknown scenario_id -> 404*
- Path: `/api/resolve`
- Request body:
```json
{
  "scenario_id": "does_not_exist",
  "weights": {
    "cost": 1.0,
    "co2": 1.0,
    "reliability": 1.0
  },
  "k_uncertainty": 1.0,
  "diesel_price_inr_per_l": 92.5
}
```
- Response (404):
```json
{
  "detail": "unknown scenario_id: does_not_exist"
}
```


## Site-scoped (multi-site)

These routes take a `site_id` (except the /api/sites collection routes) and work for any registered site, including khavda.

### `GET /api/sites`

List every registered site (always includes the khavda seed site).

No error cases.

**Response schema (200):**
Array of `SiteSummary`:
- `site_id`: string (required)
- `display_name`: string (required)
- `lat`: number (required)
- `lon`: number (required)
- `created_at`: string (required)
- `is_seed`: boolean (required)

**Example:**

*Every registered site*
- Path: `/api/sites`
- Response (200):
```json
[
  {
    "site_id": "khavda",
    "display_name": "Khavda Off-Grid Microgrid",
    "lat": 23.8443,
    "lon": 69.7317,
    "created_at": "2026-01-01T00:00:00+00:00",
    "is_seed": true
  }
]
```


### `POST /api/sites`

Register a new site, deriving a full SiteConfig from minimal input.

Example request body:
    {"display_name": "Example Site", "lat": -33.9, "lon": 18.4,
     "pv_capacity_kwp": 10.0, "battery_capacity_kwh": 30.0}

PV tilt/azimuth are derived from lat (tilt = abs(lat), array facing the
equator); wind is omitted (rated_kw=0); battery is sized at a 3-hour
C-rate; diesel reuses Khavda's combustion-property constants at the
given (or default) rated_kw; VOLL values are ratios of
outage_cost_inr_per_kwh. See core/site_store.py for the exact defaults.

No error cases beyond standard 422 body validation.

**Request body schema:**
- `display_name`: string (required)
- `lat`: number (required)
- `lon`: number (required)
- `elevation_m`: number (optional, default=0.0)
- `timezone`: string (optional, default='Asia/Kolkata')
- `pv_capacity_kwp`: number (required)
- `battery_capacity_kwh`: number (required)
- `diesel_rated_kw`: number (optional, default=0.0)
- `outage_cost_inr_per_kwh`: number (optional, default=500.0)
- `diesel_price_inr_per_l`: number (optional, default=92.5)

**Response schema (201):**
- `site_id`: string (required)
- `display_name`: string (required)
- `lat`: number (required)
- `lon`: number (required)
- `created_at`: string (required)
- `is_seed`: boolean (required)

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*Register a new site*
- Path: `/api/sites`
- Request body:
```json
{
  "display_name": "API Contract Example Site",
  "lat": -33.9,
  "lon": 18.4,
  "pv_capacity_kwp": 10.0,
  "battery_capacity_kwh": 30.0
}
```
- Response (201):
```json
{
  "site_id": "site_49016709",
  "display_name": "API Contract Example Site",
  "lat": -33.9,
  "lon": 18.4,
  "created_at": "2026-09-12T11:49:21.197237+00:00",
  "is_seed": false
}
```


### `GET /api/sites/{site_id}`

Fetch one registered site's summary.

Errors: 404 if site_id is not registered.

**Path/query parameters:**
- `site_id` (path): string — required

**Response schema (200):**
- `site_id`: string (required)
- `display_name`: string (required)
- `lat`: number (required)
- `lon`: number (required)
- `created_at`: string (required)
- `is_seed`: boolean (required)

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Examples:**

*Khavda's summary*
- Path: `/api/sites/khavda`
- Response (200):
```json
{
  "site_id": "khavda",
  "display_name": "Khavda Off-Grid Microgrid",
  "lat": 23.8443,
  "lon": 69.7317,
  "created_at": "2026-01-01T00:00:00+00:00",
  "is_seed": true
}
```

*Unknown site_id -> 404*
- Path: `/api/sites/does_not_exist`
- Response (404):
```json
{
  "detail": "unknown site_id: does_not_exist"
}
```


### `PATCH /api/sites/{site_id}`

Partially update a site's SiteConfig. Only fields present in the
request body are changed; omitted fields are left as-is.

Example request body: {"economics": {"diesel_price_inr_per_l": 100.0,
"diesel_price_source": "manual override", "co2_price_inr_per_kg": 2.0,
"voll_critical_inr_per_kwh": 500.0, "voll_essential_inr_per_kwh": 60.0,
"voll_deferrable_inr_per_kwh": 12.0}}

Errors: 404 if site_id is not registered. 400 if site_id is a seed site
(khavda) and the request touches any field other than economics,
horizon_hours, reserve_hours, or k_uncertainty -- Khavda's real hardware
configuration must not be silently rewritten by an API call.

**Path/query parameters:**
- `site_id` (path): string — required

**Request body schema:**
- `name`: string | null (optional)
- `lat`: number | null (optional)
- `lon`: number | null (optional)
- `elevation_m`: number | null (optional)
- `timezone`: string | null (optional)
- `pv`: PVSpec | null (optional)
- `wind`: WindSpec | null (optional)
- `battery`: BatterySpec | null (optional)
- `diesel`: DieselSpec | null (optional)
- `economics`: EconomicsSpec | null (optional)
- `horizon_hours`: integer | null (optional)
- `reserve_hours`: integer | null (optional)
- `k_uncertainty`: number | null (optional)

**Response schema (200):**
- `site_id`: string (required)
- `display_name`: string (required)
- `lat`: number (required)
- `lon`: number (required)
- `created_at`: string (required)
- `is_seed`: boolean (required)

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*Partial economics update on a non-seed site*
- Path: `/api/sites/site_49016709`
- Request body:
```json
{
  "economics": {
    "diesel_price_inr_per_l": 100.0,
    "diesel_price_source": "manual override (example)",
    "co2_price_inr_per_kg": 2.0,
    "voll_critical_inr_per_kwh": 500.0,
    "voll_essential_inr_per_kwh": 60.0,
    "voll_deferrable_inr_per_kwh": 12.0
  }
}
```
- Response (200):
```json
{
  "site_id": "site_49016709",
  "display_name": "API Contract Example Site",
  "lat": -33.9,
  "lon": 18.4,
  "created_at": "2026-09-12T11:49:21.197237+00:00",
  "is_seed": false
}
```


### `DELETE /api/sites/{site_id}`

Permanently delete a site: removes its registry entry and its own
config/scenarios/runs directory (sites/{site_id}/).

Errors: 404 if site_id is not registered. 400/403 if site_id is a seed
site (khavda) -- seed sites can never be deleted through this API.

**Path/query parameters:**
- `site_id` (path): string — required

**Response schema (204):**
*(no content)*

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Examples:**

*Seed sites can never be deleted -> 400*
- Path: `/api/sites/khavda`
- Response (400):
```json
{
  "detail": "site 'khavda' is a seed site and cannot be deleted"
}
```

*Delete a non-seed site -> 204*
- Path: `/api/sites/site_49016709`
- Response (204):
```json
null
```


### `GET /api/sites/{site_id}/scenarios`

List the scenarios available for a site.

For khavda, returns the human-named S1/S2/S3 scenarios defined in
config/scenarios.yaml. For any other site, returns one entry per
scenario parquet file found under that site's own scenarios directory
(id and name are the same string, since new sites have no separate
human-naming step yet) -- an empty list if none have been uploaded.

Errors: 404 if site_id is not registered.

**Path/query parameters:**
- `site_id` (path): string — required

**Response schema (200):**
Array of `ScenarioSummary`:
- `scenario_id`: string (required)
- `name`: string (required)

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*Khavda's named scenarios*
- Path: `/api/sites/khavda/scenarios`
- Response (200):
```json
[
  {
    "scenario_id": "S1",
    "name": "Normal week"
  },
  {
    "scenario_id": "S2",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S3",
    "name": "Lean season"
  }
]
```


### `GET /api/sites/{site_id}/runs`

List every precomputed dispatch run for any registered site.

Same shape as legacy GET /api/runs, generalized to site_id. A site with
no precomputed runs yet (e.g. one just created via POST /api/sites)
returns an empty list, not an error.

Errors: 404 if site_id is not registered.

**Path/query parameters:**
- `site_id` (path): string — required

**Response schema (200):**
Array of `RunListEntry`:
- `scenario_id`: string (required)
- `policy`: string (required)
- `name`: string (required)

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*Khavda's precomputed runs, via the site-scoped route*
- Path: `/api/sites/khavda/runs`
- Response (200):
```json
[
  {
    "scenario_id": "S1",
    "policy": "diesel_only",
    "name": "Normal week"
  },
  {
    "scenario_id": "S1",
    "policy": "mpc",
    "name": "Normal week"
  },
  {
    "scenario_id": "S1",
    "policy": "perfect_foresight",
    "name": "Normal week"
  },
  {
    "scenario_id": "S1",
    "policy": "rule_based",
    "name": "Normal week"
  },
  {
    "scenario_id": "S2",
    "policy": "diesel_only",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S2",
    "policy": "mpc",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S2",
    "policy": "perfect_foresight",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S2",
    "policy": "rule_based",
    "name": "Monsoon stretch"
  },
  {
    "scenario_id": "S3",
    "policy": "diesel_only",
    "name": "Lean season"
  },
  {
    "scenario_id": "S3",
    "policy": "mpc",
    "name": "Lean season"
  },
  {
    "scenario_id": "S3",
    "policy": "perfect_foresight",
    "name": "Lean season"
  },
  {
    "scenario_id": "S3",
    "policy": "rule_based",
    "name": "Lean season"
  }
]
```


### `GET /api/sites/{site_id}/runs/{scenario_id}/{policy}`

Fetch one precomputed dispatch run for any registered site.

Example: GET /api/sites/khavda/runs/S2/mpc

Errors: 404 if site_id is not registered, or if no precomputed run
exists for that scenario_id/policy pair on this site.

**Path/query parameters:**
- `site_id` (path): string — required
- `scenario_id` (path): string — required
- `policy` (path): string — required

**Response schema (200):**
- `scenario_id`: string (required)
- `policy`: string (required)
- `site_id`: string (required)
- `site_name`: string (required)
- `steps`: list[StepResult] (required)
- `kpi`: KPI (required)
- `provenance`: Provenance (required)
- `notes`: string (optional, default='')

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Examples:**

*Existing precomputed run, via the site-scoped route*
- Path: `/api/sites/khavda/runs/S2/mpc`
- Response (200):
```json
{
  "scenario_id": "S2",
  "policy": "mpc",
  "site_id": "khavda_01",
  "site_name": "Khavda Off-Grid Microgrid",
  "steps": [
    {
      "t": "2025-09-03T00:00:00+05:30",
      "pv_kw": 0.0,
      "wind_kw": 0.46453875011030227,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": 6.907007896208329,
      "soc_kwh": 64.72946537241228,
      "soc_pct": 53.9412211436769,
      "load_critical_kw": 1.2,
      "load_essential_kw": 6.171546646318631,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R6_AVOID_START",
      "reason_text": "Avoiding a generator start; the battery covers this gap."
    },
    {
      "t": "2025-09-03T01:00:00+05:30",
      "pv_kw": 0.0,
      "wind_kw": 0.0997540869082972,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": 7.084458048787209,
      "soc_kwh": 57.272141110531,
      "soc_pct": 47.72678425877584,
      "load_critical_kw": 1.2,
      "load_essential_kw": 5.984212135695506,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 0.0,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R6_AVOID_START",
      "reason_text": "Avoiding a generator start; the battery covers this gap."
    },
    "... (168 steps total, truncated for this doc)"
  ],
  "kpi": {
    "diesel_l": 403.0769478809622,
    "cost_fuel_inr": 37284.61767898901,
    "cost_total_inr": 41754.75973673548,
    "co2_kg": 1080.2462203209789,
    "renewable_frac": 0.5667797555434724,
    "unserved_critical_kwh": 0.0,
    "unserved_total_kwh": 29.970801425376486,
    "critical_outage_hours": 0.0,
    "dg_starts": 13,
    "dg_run_hours": 67.0,
    "batt_equivalent_full_cycles": 4.4355792895730195,
    "cost_per_delivered_kwh_inr": 16.923106208210587,
    "served_kwh": 2467.3224420512893,
    "solve_ms_mean": 198.40602559522614,
    "solve_ms_p95": 350.1660250003624
  },
  "provenance": {
    "weather_source": "open-meteo-archive",
    "forecast_method": "ar1-lead-scaled-noise-v1",
    "load_method": "synthetic-seeded-v1",
    "diesel_price_source": "PPAC Gujarat retail selling price \u2014 PLACEHOLDER, verify before demo",
    "config_hash": "c8b5f984",
    "generated_at": "2026-09-11T22:59:13.875116+00:00",
    "code_version": "phase3"
  },
  "notes": ""
}
```

*A brand-new site has no precomputed runs yet -> 404*
- Path: `/api/sites/site_49016709/runs/S1/mpc`
- Response (404):
```json
{
  "detail": "no precomputed run for S1/mpc"
}
```


### `POST /api/sites/{site_id}/solve_live`

Same as legacy POST /api/solve_live, generalized to any registered
site's own hardware/economics configuration.

Errors: 404 if site_id is not registered; otherwise same as legacy
/api/solve_live (never raises on a weather-fetch failure).

**Path/query parameters:**
- `site_id` (path): string — required

**Request body schema:**
- `lat`: number (required)
- `lon`: number (required)
- `soc_pct`: number (required)

**Response schema (200):**
- `scenario_id`: string (required)
- `policy`: string (required)
- `site_id`: string (required)
- `site_name`: string (required)
- `steps`: list[StepResult] (required)
- `kpi`: KPI (required)
- `provenance`: Provenance (required)
- `notes`: string (optional, default='')

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*Live 72h plan for the new site's own hardware/coordinates*
- Path: `/api/sites/site_49016709/solve_live`
- Request body:
```json
{
  "lat": -33.9,
  "lon": 18.4,
  "soc_pct": 50.0
}
```
- Response (200):
```json
{
  "scenario_id": "LIVE",
  "policy": "mpc",
  "site_id": "site_49016709",
  "site_name": "API Contract Example Site",
  "steps": [
    {
      "t": "2026-09-12T17:00:00+05:30",
      "pv_kw": 2.0947761,
      "wind_kw": 0.0,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": -0.89477615,
      "soc_kwh": 15.850037,
      "soc_pct": 52.83345666666666,
      "load_critical_kw": 1.2,
      "load_essential_kw": 10.992984087278119,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 10.992984,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 4.960439126122651e-08,
      "fuel_l": 0.0,
      "reason_code": "R2_SOLAR_SURPLUS",
      "reason_text": "Charging the battery from surplus solar and wind."
    },
    {
      "t": "2026-09-12T18:00:00+05:30",
      "pv_kw": 6.2431891,
      "wind_kw": 0.0,
      "dg_kw": 0.0,
      "dg_on": 0,
      "batt_kw": -5.0431891,
      "soc_kwh": 20.641067,
      "soc_pct": 68.80355666666667,
      "load_critical_kw": 1.2,
      "load_essential_kw": 21.465621064542027,
      "load_deferrable_kw": 0.0,
      "unserved_critical_kwh": 0.0,
      "unserved_essential_kwh": 21.465621,
      "unserved_deferrable_kwh": 0.0,
      "curtailed_kwh": 0.0,
      "fuel_l": 0.0,
      "reason_code": "R2_SOLAR_SURPLUS",
      "reason_text": "Charging the battery from surplus solar and wind."
    },
    "... (72 steps total, truncated for this doc)"
  ],
  "kpi": {
    "diesel_l": 0.0,
    "cost_fuel_inr": 0.0,
    "cost_total_inr": 62264.928438,
    "co2_kg": 0.0,
    "renewable_frac": 1.0,
    "unserved_critical_kwh": 26.9827737,
    "unserved_total_kwh": 964.6751335,
    "critical_outage_hours": 16.0,
    "dg_starts": 0,
    "dg_run_hours": 0.0,
    "batt_equivalent_full_cycles": 1.2585986900633335,
    "cost_per_delivered_kwh_inr": 575.723386544113,
    "served_kwh": 108.15077152199225,
    "solve_ms_mean": 67.67299999773968,
    "solve_ms_p95": 67.67299999773968
  },
  "provenance": {
    "weather_source": "open-meteo-forecast-live",
    "forecast_method": "open-meteo-forecast-live",
    "load_method": "synthetic-seeded-v1",
    "diesel_price_source": "manual override (example)",
    "config_hash": "be37ea44",
    "generated_at": "2026-09-12T11:49:22.458493+00:00",
    "code_version": "phase4"
  },
  "notes": "Live 72h forecast-driven plan (forecast_method=open-meteo-forecast-live)."
}
```


### `POST /api/sites/{site_id}/resolve`

Same as legacy POST /api/resolve, generalized to any registered
site's own scenario data (looked up as
<that site's scenarios_dir>/{scenario_id}.parquet).

Errors: 404 if site_id is not registered, or if that site has no
parquet data for the given scenario_id (e.g. a newly created site with
no scenarios uploaded yet).

**Path/query parameters:**
- `site_id` (path): string — required

**Request body schema:**
- `scenario_id`: string (required)
- `weights`: ResolveWeights (optional, default={'cost': 1.0, 'co2': 1.0, 'reliability': 1.0})
- `k_uncertainty`: number (required)
- `diesel_price_inr_per_l`: number (required)

**Response schema (200):**
- `scenario_id`: string (required)
- `policy`: string (required)
- `site_id`: string (required)
- `site_name`: string (required)
- `steps`: list[StepResult] (required)
- `kpi`: KPI (required)
- `provenance`: Provenance (required)
- `notes`: string (optional, default='')

**Possible status codes beyond 200:** 422 (422 = request body/params failed validation; see the description above for endpoint-specific error meanings).

**Example:**

*A brand-new site has no scenario data yet -> 404 (fast, no MILP solve)*
- Path: `/api/sites/site_49016709/resolve`
- Request body:
```json
{
  "scenario_id": "S1",
  "weights": {
    "cost": 1.0,
    "co2": 1.0,
    "reliability": 1.0
  },
  "k_uncertainty": 1.0,
  "diesel_price_inr_per_l": 92.5
}
```
- Response (404):
```json
{
  "detail": "no scenario data for S1"
}
```

